# Mean repository policy

This repository owns reusable CSI capture and analysis implementation only.
Every writing command must receive an explicit experiment-owned output path; do
not add plans, datasets, runs, results, or location observations here.
